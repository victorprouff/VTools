services:
  vtools:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - ConnectionStrings__DefaultConnection=Data Source=/data/vtools.db
    restart: unless-stopped
